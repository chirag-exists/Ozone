package com.example.offgridmesh.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette ──
val MeshGreen = Color(0xFF00E676)
val MeshGreenDark = Color(0xFF00C853)
val MeshGreenSubtle = Color(0xFF1B5E20)

// ── Secondary / Accent ──
val MeshCyan = Color(0xFF00BCD4)
val MeshCyanDark = Color(0xFF0097A7)

// ── Backgrounds ──
val DeepNavy = Color(0xFF0A0E1A)
val DarkSurface = Color(0xFF131829)
val CardSurface = Color(0xFF1A2035)
val ElevatedSurface = Color(0xFF212845)

// ── Text ──
val TextPrimary = Color(0xFFE8EAED)
val TextSecondary = Color(0xFFB0B3B8)
val TextMuted = Color(0xFF6B7280)

// ── Semantic ──
val ErrorRed = Color(0xFFFF5252)
val WarningAmber = Color(0xFFFFAB40)

// ── Chat Bubbles ──
val OwnBubble = Color(0xFF0D3B2E)
val OwnBubbleBorder = Color(0xFF00E676)
val PeerBubble = Color(0xFF1A2035)
val PeerBubbleBorder = Color(0xFF2A3150)

// ── Misc ──
val DividerColor = Color(0xFF1E2540)
val ShimmerHighlight = Color(0xFF2A3150)
